// Canonical location per spec section 2.10.
// The file actually served to the browser is static/calendar/breakpoints.js;
// keep these two in sync.
window.SGDR_BREAKPOINTS = {
  PHONE_MAX_WIDTH: 768,
  PHONE_DAY_DENSITY_THRESHOLD: 4,
};
